<div class="container">
<form action="/board/create" method="POST" style="margin-top: 100px">
    <div class="form-group">
        <label for="exampleInputEmail1">제목</label>
        <input type="text" name="title" class="form-control" placeholder="제목">
    </div>
    <div class="form-group">
        <textarea class='form-control' name='content' placeholder="내용을 입력하세요"> </textarea>
    </div>
    <button type="submit" class="btn btn-default">글쓰기</button>
</form>

<br> <br> <br> 
<div class="container" style="margin-top:50px">
    <h3 style="text-align: center">대화창</h3>

    <% @posts.each do |post| %>
    <hr>
      <%= post.title %> <br>
      <%= post.content %> <br>
      <%= post.created_at %> <br>
      모든 글자 수: <%=post.content.length-1 %> <br>
      공백 없는 글자 수: <%= post.content.length - post.content.count(' ') %><br>
      단어 수: <%=post.content.split(' ').length %><br>
      <br>
      <% post.comments.each do |comment| %>
          <%= comment.id %>
          <%= comment.content %> <br>
      <%end%>
      <br>
      <form action='/board/<%=post.id%>/comment_create' method="POST">
        <div class="form-group">
            <input type="text" class="form-control" name="content" placeholder="댓글">
        </div>
        <button type="submit" class="btn btn-default">댓글 달기</button>
      </form>

    <%end%>
    <hr>
</div>
</div>